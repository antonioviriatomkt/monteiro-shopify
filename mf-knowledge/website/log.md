# Update Log

Changes made to **monteirofabrics.com**, newest first. Scope: the live website (its theme, pages,
metadata and redirects). This is distinct from the bundle-level [root log](/log.md), which records
changes to this knowledge bundle.

## 2026-09-10

* **ORGATEC 2026 landing page published on the live theme** — `/pages/orgatec-2026` (page id `718361624953`, DE + EN; PT/FR/ES/IT fall back to EN). **First deliberate exception to "never edit live":** two *new* files pushed to `Showcase 7.0` via CLI (`--allow-live --nodelete --only`) — `sections/orgatec-2026.liquid` (self-contained: copy in both languages switched on `request.locale`, own CSS scoped to `.o26`, native `{% form 'contact' %}` for stand meetings with hidden UTM/gclid fields filled by JS, client-built `.ics`, `Event` JSON-LD, `phase` setting pre/live/post) and `templates/page.orgatec-2026.json`. No existing live file touched (verified by `updatedAt` on `index.json`, `landing-cta.liquid`, `page.tradeshow_landing_page.json`). Same two files pushed to the sandbox `195386114425` and committed (`92a28a1`) so the redesign publish keeps the page. DE title/meta/body registered with `translationsRegister`. **Not reused:** the Techtextil template `page.tradeshow_landing_page.json` (António: build from scratch, optimised for the campaign). **Learned:** the Shopify MCP connector refuses `themeFilesUpsert` on the MAIN theme — live pushes go through the CLI, which needs `--allow-live` non-interactively; the theme centres `h1–h3` and forces their colour, so any section with light-on-dark headings needs `text-align:left; color:inherit`. Collection cards transcribe the stand panels (CHANCE 2.0 · Mediflex · Crib 7|B1 · Bioblend · ELECTRA · STRAW · ORIGIN · PU Office · PVC Fabric Look); CHANCE 2.0 grade collections are 404 on live, so those cards link to `/pages/chance-collection` and `/products/mediflex-collection`. **Open:** hero image (Ivana's visual — `hero_image` setting, mosaic fallback until then), a test submission of the meeting form, GTM triggers on `data-cta` / `contact_posted=true`, switch `phase` on 27/10 and 31/10. **Later the same day:** body switched off at António's request — new `show_body` setting (default off), page renders event bar + hero only; hero secondary CTA falls back to `/pages/contact`. Commit `1b56b1a`, pushed to live and sandbox.

* **ORGATEC LP v2 built, not pushed** — António rejected the v1 design as too far from the live site's design language. `sections/orgatec-2026.liquid` rewritten from the live theme's own tokens (Source Sans Pro, `#464654` buttons via the theme's `.button`, 48px light headings, `#f5f5f5` CTA band, swatch cards, house accordion), generated from the vault prototype `Clients/Monteiro-Fabrics/prototypes/orgatec-2026/index.html` by `scripts/orgatec-2026-build-liquid.py`. CHANCE 2.0 promoted without a sample CTA (launches with the new site); sample CTAs on ELECTRA · STRAW · ORIGIN · PU Office · PVC Fabric Look; sector tiles to the campaign-A collections; meeting form kept as secondary conversion; `show_body` setting dropped. Commit `925794f` — **live and sandbox still carry v1 (hero only)**. Same day António asked for a **sector-led v3** (sectors Monteiro specifies for that overlap with ORGATEC's, then the collections per sector; no colour tiles) — in progress.

## 2026-08-20

* **Global `Organization` + `WebSite` node added to the theme** (commit `784f909`, local — sandbox not pushed). The site marked its **pages** but never its **company**: **zero** `ld+json` in `layout/theme.liquid`, **zero** `sameAs` anywhere in the theme, and the only `WebSite` inlined **without `@id`** inside `CollectionPage.isPartOf` (`sections/faq-schema.liquid:75`). Nothing could resolve that the site, the LinkedIn page and the Instagram account are one entity. Added **`snippets/structured-data-organization.liquid`** — a `@graph` with `Organization` (`@id …/#organization`) and `WebSite` (`@id …/#website`, `publisher`-linked) — rendered from the `<head>` on **every** page, right after `social-meta-tags`. **Values are sourced, not guessed:** `sameAs` = `linkedin.com/company/monteiro-fabrics` + `instagram.com/monteirofabrics` (confirmed by António); `address` = *Circunvalação, 9020 · Apartado 52511 · 4202-351 Porto · PT*, read verbatim off the live `/pages/contact` and cross-checked against `/pages/about-us`; **two** `contactPoint`s — PT `+351 228 338 640` and DE `+49 171 4101578` (`areaServed: DE`, matching the Performance Fabrics campaign), both with `hello@monteirofabrics.com`. **Oberhaching is modelled as a second `contactPoint`, not a second `address`** — a second address would imply a second legal entity. `logo` = `settings.logo` (a real PNG in `shop_images`, not a favicon). Every optional field is emitted **only when present**, so a partially filled version stays valid instead of leaking placeholders. **Validated:** rendered JSON parses clean with *and* without a logo (2 nodes, 2 `sameAs`, 2 `contactPoint`, publisher resolving to the org `@id`); `shopify theme check` holds at the theme's 275 pre-existing offenses — **0 new**. **Still open:** repoint `faq-schema.liquid`'s `isPartOf` at the `WebSite` `@id`; Ivana to confirm `settings.logo` is the institutional mark and not a layout lockup.

* **🚨 Found, decided, deliberately NOT fixed — the live site declares its fabrics free.** `snippets/structured-data-product.liquid` feeds `variant.price` straight into the `Offer`, and **every variant of all 15 active products is priced `0.00`** (verified via the Shopify connector: AMAZÓNIA, BETA, CHANCE [1/7]–[7/7], ELECTRA, ELEFANTE, FLORA, FURNA, GARBE, GLAM — `minVariantPrice: 0.0 EUR` throughout). The cart is a **free-sample mechanism** — the product page CTA is *"Add free sample to cart"* — not a shop. So every product page publishes `"price": 0.0, "priceCurrency": "EUR", "availability": "InStock"` in machine-readable form: a claim that the fabrics **cost nothing and are in stock**, which is worse than silence because an LLM will repeat it. **This is on the live `Showcase 7.0` theme, not just the sandbox — it predates the redesign.** Decision: **`Offer` stops carrying a price** (drop `price` + `priceCurrency`, keep `url`/`availability`, point `seller` at the org `@id`); the free sample is **not** modelled as a €0 offer of the fabric, since conflating the two is the defect itself. **António decided the same day not to patch the live theme** — the never-edit-live rule holds and the fix ships with the redesign push. **Consequence accepted knowingly:** the €0 claim stays in production until a launch that has no date. → decision note `2026-08-20-offer-sem-preco`, work queued in the `schema-product-additional-property` task.

* **Structured-data gap audit (no site change).** Verified across the theme, for the record: of **45** `page.*.json` templates, exactly **3** carry any JSON-LD (`page.chance-lp`, `page.chance-collection`, `page.performance-fabrics`) — the 8 signature collection pages and the new `/pages/sectors` hub have **none**. `structured-data-article.liquid` builds `publisher.logo` from **`page_image`**, so every Technical Hub post declares its own cover photo as the company logo — a defect, not an omission. `Product` emits **zero `additionalProperty`** while abrasion, fire, colour-fastness, alcohol and antibacterial values sit filled in `custom.*` metafields and render as prose duplicated across **6** product templates (which also carry a customer-visible typo, **"Abrasian Resistance"**). `templates/robots.txt.liquid` is Shopify's default groups plus `Allow: /policies/` — **no AI crawler is blocked**, so there is no access remedy to make.

## 2026-08-18

* **Sector hubs consolidated into one — "Our Sectors" at `/pages/sectors`** (decision D-a). The site had **four** pages doing the same job; only two were actually published and competing (`/pages/fabrics-by-industry`, `/pages/by-application`), `/pages/sectors` existed but was **unpublished**, and `page.our-sectors.json` was an **orphan template with no page at all**. Built `templates/page.sectors.json` as a **byte-perfect copy** of `page.all-collectby-industry.json` (`themeFilesCopy`, md5 `0a11aaf8d50b1397b145dcf774081ce9`, 6861 B — the rich hub: hero "Our Sectors" + 16 enabled tiles + stock service + certifications + closing CTA). Page `sectors` retitled **"Sectors" → "Our Sectors"**, `templateSuffix: sectors`. **Repointed 7 references across 6 templates** `fabrics-by-industry` → `sectors`: `collection.json` (⚠️ the **default collection template**), `index.json` (homepage "View all"), `collection.colecoes-taxonomia`, `collection.colecoes-pradao` (inline `<a href>`), `collection.test`, `page.all-collections-catalogue` (×2). Pushed and **verified server-side: 0 stale references remain**. **Deliberately left for launch day:** `/pages/sectors` stays **unpublished** (publishing now would serve a blank page on live Showcase 7.0, which has no `page.sectors.json`), and the **301s are not created** (firing pre-publish would bounce live pages into sandbox-only content). Pending: the SEO title tag (H1 stays "Our Sectors"; the title tag should carry keyword).
* **Homepage de-scoping reverted.** `performance_fabrics_feature` and `chance_2_feature` are **enabled** and stay enabled. Disabling them (2026-07-15) was a contingency for launching *before* PF and Chance 2.0 were ready; the client chose to launch with everything ready, so it no longer applies.
* **Drift recovered — the sandbox was AHEAD of local.** A CLI pull + diff found four templates where local had **lost the entire 2026-07-02 Train workstream** despite carrying newer mtimes: `page.all-collectby-industry.json` (`s_train` tile, disabled), `page.all-collect-by-applicabil.json`, `page.mission-vision.json` (train collection tiles), `page.mobility.json` (TRAINS image block). Pushing from local as it stood would have silently deleted that work. `index.json` was **in sync** — an Admin-API checksum/size mismatch on it was stored-bytes vs CLI formatting, not drift. **The APP repo is now under git** (baseline `bfea142`), so drift checks are a `git diff` from here on.
* **Housekeeping noted, not actioned:** two stray `assets/styles.css.liquid.tmp.*` files left on the sandbox by a theme-editor save; `page.our-sectors.json` and (at launch) `page.all-collectby-industry.json` / `page.all-collect-by-applicabil.json` become orphan templates.

## 2026-07-15

* **Launch put on hold (strategic — was unlogged).** Confirmed with the client: **Ivana has decided to hold the entire CLAUDE_MF_JUNE_PROPOSTA launch until all marketing material is available** — no target date; we wait for the material, finish the outstanding content, then publish everything together. This supersedes the migration audit's launch scope (`briefs/migration-readiness-audit.md`), which assumed an imminent swap. **Two content workstreams gate the launch:** (1) **Chance 2.0** — colour samples + catalogue/copy (see the Chance 2.0 point-of-situation below), (2) **Performance Fabrics** — the PF landing page still needs its content (R&D/pilot facts, PFAS substantiation, capacity figures). The migration/IA decisions (Sectors hub URL, redirects, About-Us merge) are **not** the gating item per the client.
* **De-scoping already applied on the sandbox (was unlogged).** Ahead of the (now-held) launch, **Performance Fabrics and Chance 2.0 were made invisible on the homepage** — `templates/index.json` has both `performance_fabrics_feature` **and** `chance_2_feature` set `disabled: true` (verified). **The Technical Hub blog + articles stay live** (they were never store-level-off). The plan had been to go live *without* the PF page and *without* the Chance 2.0 collection, keeping the original Chance with a stop-gap editorial LP (`/pages/chance-lp` — the "CHANCE / 236 SHADES" page). **That stop-gap page is now shelved / no longer needed:** the decision is to **keep the Chance 2.0 system in place and finish it** (colours + catalogue/copy) rather than revert to original Chance. See [Chance 2.0](/website/chance-2-0.md).
* **Sandbox drift sync (local source ↔ live).** Pulled CLAUDE_MF_JUNE_PROPOSTA via CLI and diffed against `APP/theme/`. **Live was ahead of local in 15 files** (all theme-editor edits by the client, none previously logged); synced the pulled versions into `APP/theme/` so the source of truth is trustworthy again. Byte-identical everywhere else (all 61 sections, 84 snippets, `styles.css.liquid`, locales, blocks). **Real client edits captured:** a **"DOWNLOAD CATALOGUE"** CTA → the CHANCE 2.0 catalogue PDF (`MR_web_cat_CHANCE_2.0_JUN2026_Ipad_bx.pdf`) added across the **Chance 2.0 hub + all 6 grade pages**; hub hero **"236 shades" → "236 colours"**; `chance-crib-5` featured product **price block removed**; **Contact** page email **`info@` → `hello@monteirofabrics.com`**, hours **9:00 → 8:00**, **"Fashion"** added to the sector dropdown; **Tailor-made** title **"Custom Solutions" → "Tailor-made Solutions"**; **About-Us** hero subheading **"Manufacturer of coated fabric since 1967" → "Over 100 years of Industrial Legacy"** + an **Official Resellers** section added; `page.origin` hero images resized; two Our-Sectors tiles (`page.all-collectby-industry`) disabled; `page.straw` `spec_icons` reordered; `en.default.json` collection empty-state copy changed. The stray local `assets/styles.css` (compiled artifact, never deployed) was left as-is. **⚠ Still store-level, not verifiable from theme files:** confirm the live nav `mf_june_menu` also drops the Chance 2.0 / PF entries (menus live in store Navigation, not the theme).

## 2026-07-14

* **Homepage "View all" → Sectors (bug fix).** The homepage "Solutions by industry & application" (`collection-list`) "View all" button went to `/collections` because `sections/collection-list.liquid` hard-coded `href="{{ routes.collections_url }}"` with no override. Added an optional **`view_all_link`** URL setting (defaults to the all-collections route, so other `collection-list` uses are unchanged) and pointed the homepage section at **`/pages/fabrics-by-industry`** (`templates/index.json`) — the **actual live Our Sectors page** (`all-collectby-industry` template). (First pushed pointing at `/pages/sectors`, but that page is a thin default-template placeholder — corrected.) Pushed via CLI, validated. **Also corrected the blocker-3 (2026-07-13) repoints** (same wrong assumption): the sector links previously moved to `/pages/sectors` — `collection.json` (default template), `collection.colecoes-taxonomia`, `collection.test`, `page.all-collections-catalogue` (×2), `collection.colecoes-pradao` ("Explore all sectors") — **all repointed to `/pages/fabrics-by-industry`** and pushed (validated, 0 `pages/sectors` left in the theme). **Still open (launch-time IA decision):** whether to consolidate `by-industry` + `by-application` into a clean `/pages/sectors` hub, or keep `fabrics-by-industry` as the Our Sectors page — the migration audit §2.1 recommendation (`/pages/sectors`) needs revisiting against this reality.
* **Tooling + CHANCE 2.0 groundwork (no live-site change).** Switched Shopify Admin API access to the **CLI `shopify store execute`** path (full read+write, see memory [[shopify-admin-access]]) — the create_page token (read_orders only) and the Shopify MCP connector (net::ERR_FAILED) are both dead. Ingested the **CHANCE 2.0 catalogue (DC 073.01)** → `briefs/chance-2-0-catalogue-data.md` (+ colours in `briefs/chance-2-0-grade-colours.md`); confirmed the 6 grade collection pages already carry **datasheet-accurate specs + a colour `variant_picker`** (no `[TO CONFIRM]`). Remaining CHANCE work is narrow: swatches (none exist on any variant), Crib 5/7 colour curation (34→22, 24→9), a dedicated Bioblend product, cosmetic product/colour renames.
* **CHANCE 2.0 — first live product write.** Renamed the Mediflex colour variants **Desert→Dune, Ocean→Lagoon, Shale→Oasis** (`productOptionUpdate` via `store execute`, 0 errors) — the confirmed Mediflex renames. Rest of the colour work (swatches, Crib 5/7 curation, product renames, dedicated Bioblend product) pending Ivana's colour cards / decisions.

## 2026-07-13

* **Correction — Peel is NOT part of CHANCE 2.0 (repoints reverted).** Per the client: `chance-bioblend` is a **separate new collection**, *not* a Peel rebrand — Peel stays live at `/pages/peel-collection`. **Reverted** the 5 `peel-collection → chance-bioblend` internal-link repoints from the migration-prep pass below (`page.inspiration`, `list-collections`, `collection.colecoes`, `collection.colecoes-applicability`, `page.collections-chance-peel` → back to `shopify://pages/peel-collection`); re-validated + pushed via CLI, verified. **Fixed the wrong "Bioblend = Peel" mapping** everywhere it was recorded: `briefs/seo-cannibalization-remediation-plan.md` (AT-LAUNCH CHANCE item + Cluster 3), `briefs/migration-readiness-audit.md`, `mf-knowledge/website/chance-2-0.md`, and memory [[collection-seo]] + [[mf-redesign-launch-seo-queue]]. The other four grade mappings (ocean/mediflex/prosoft/portus) are unchanged. **⚠ Open:** the `chance-bioblend` collection template still references the **peel-collection product** (`collection.chance-bioblend.json:54`) — confirm what Bioblend actually contains now it's separate from Peel.

* **Migration prep — hard-blockers fixed + internal links repointed off retiring URLs.** Started the pre-launch migration-readiness pass (`briefs/migration-readiness-audit.md`). Pulled the sandbox via CLI first. **(1)** Fixed the homepage hero's **dead primary CTA** (`index.json` — "Speak to our specialists" had an empty `button_link` → `shopify://pages/contact`). **(2)** Stripped the customer-visible **`[TO CONFIRM]`** from the OEKO-TEX spec on `page.performance-fabrics.json` (kept the "OEKO-TEX Standard 100" claim per Ivana). **(3) Repointed internal links feeding to-be-retired URLs** so their launch-day 301s don't fire off live internal links: `pages/by-application` + `pages/fabrics-by-industry` → **`/pages/sectors`** (incl. the **default collection template** `collection.json`, plus `page.all-collections-catalogue.json`, `collection.colecoes-taxonomia.json`, `collection.colecoes-pradao.json`, `collection.test.json`); `pages/quality` → **`/pages/about-us`** (`page.taxonomia.json`, `page.faq.json` "Certifications" link); `pages/peel-collection` → **`/collections/chance-bioblend`** (peel→Bioblend) across the structural templates (`list-collections.json`, `collection.colecoes.json`, `collection.colecoes-applicability.json`, `page.inspiration.json`, `page.collections-chance-peel.json`). **14 templates edited**, JSON-validated (JSONC — must strip the `/* */` header before parsing), pushed via CLI (`--only`, `--nodelete`), round-trip verified. **Deferred:** peel-collection links inside legacy blog/campaign article templates (`post-artigo-luisa`, `public-blogue-gisela`, `pvc-fabrics`, `blog-post-bioshoes4all`, `trade-fair-invitation`) — repoint as cleanup only if those articles are live; `pages/mediflex` links — pending the CHANCE 2.0 product-URL decision. **Still open:** `/pages/sectors` needs its hub template + full sector-tile set assigned (currently default/thin). Sector-hub target set to `/pages/sectors` (vs `/pages/our-sectors`) per the audit recommendation.

* **White paper — technical review round 2 (6 copy corrections, delivered for manual apply).** Ivana's specialist returned a second round of technical feedback on the Technical Hub pillar (`/blogs/technical-hub/complete-guide-to-healthcare-upholstery-fabrics`, article id 1007373713785). The custom-app token still lacks content scope (`read_content`/`write_content`) so `articleUpdate` is blocked — the corrections were handed to the client as **find/replace text** to paste into the live article. **The six edits:** (1) **ISO 22196 vs 20743 are not interchangeable** — split across ~5 mentions: **ISO 22196 = coated/non-porous surfaces**, **ISO 20743 = textiles** (killed the `22196/20743` shorthand). (2) **Fire (Section 3)** — dropped "requires Crib 5"; reframed **region-aware**: EN 1021-1/-2 for continental Europe, **BS 7176** Crib 5/7 for the UK; kept the composite-system point (the old copy undervalued Mediflex, which passes EN 1021-1&2). (3) **Durability** — `EN ISO 12947` → **ISO 5470-2** for coated fabrics (12947 is the textile method) across ~4 mentions. (4) **FAQ "What fire rating do healthcare fabrics need?"** — rewritten more robustly around **BS 7176** (Medium Hazard = Crib 5 / High = Crib 7), EN 1021-1/-2, DIN 4102-1 B1 and composite testing (replaces the earlier BS 5852-only draft). (5) **Spec checklist + cert quick-reference table** — harmonised to the same BS 7176 framing so nothing contradicts the FAQ. (6) **FAQ "What documentation should I request from a supplier?"** — added a conditional **ISO 10993 biocompatibility** line (cytotoxicity + skin irritation) for medical-device / prolonged-skin-contact cases. **Consistency:** items 2, 4 and 5 are the BS 7176 set and must be applied together. **Still open:** the standalone **Crib 5 vs Crib 7** and **B1** articles were not re-checked for the same BS 7176 / EN 1021 framing. Notion "Website Page Review" white-paper row → Feedback #2 filled, Status **Changes requested**. See [[collection-seo]].

## 2026-07-07

* **Electra page restructured** (`page.electra.json`, `/pages/electra-medical-upholstery`). Was disorganised — generic "High-performance Fabrics" opener, the antistatic wedge buried, benefits split across 5 disconnected blocks, no comparison. Rebuilt around Electra's unique value: **the only Monteiro healthcare fabric that is permanently antistatic / ESD-protective** (ICU, imaging, OR). New flow: hero (wedge line) → antistatic positioning → "Why static is a hazard" → **differentiator** (anatomy + resistivity 10⁵–10⁶ Ω) → **NEW "Electra vs a standard healthcare fabric" comparison table** → consolidated **"Built for clinical use"** strengths grid (merged the two icon strips + biocompatibility) → palette "antistatic, in colour" → **NEW technical spec-table** (datasheet-backed from `electra.md`: PVC 88/PA 12, 600 g/m², 10⁵–10⁶ Ω, 150,000 Martindale/ISO 5470-2, fire list, ISO 10993, REACH, LAV VVV+) → product → Mediflex + Healthcare cross-links → CTA. Dropped 2 fragmented sections. Deployed via CLI, round-trip verified. **Gotcha:** the theme's `rich-text` `text` is a *restricted richtext* (only `<p>/<ul>/<ol>/<h1-6>`, no `<table>`/`style`) — HTML tables must go in the **`custom-liquid`** section (setting `custom_liquid`, type "liquid").
* **Performance Fabrics — tile links finalised + homepage CTA added.** Finalised the 3 sector-tile links on `/pages/performance-fabrics` (cleared the `[TO CONFIRM link]` notes; **Medical vehicles** re-pointed `automotive` → `/collections/medical` — the hygienic/fluid-proof match; Stadiums → public-spaces and Safety equipment → contact kept). Added a **Performance Fabrics feature** to the homepage (`templates/index.json`): an `image-with-text` band ("Certified technical fabrics for the toughest specifications" → *Explore Performance Fabrics* → `/pages/performance-fabrics`, LAB-FABRICS image) inserted **immediately after the "Engineered for performance" (`performance-tech`) section**. Deployed via CLI, round-trip verified. **PF now at 3 markers:** OEKO-TEX (kept per Ivana) + PFAS-free ×2 (spec + FAQ, awaiting the technical team).
* **`[TO CONFIRM]` cleanup — datasheet-backed values applied.** Cross-checked the 21 markers against the `mf-knowledge` product datasheets. **Filled 12** with sourced values: Performance Fabrics (`page.performance-fabrics.json`) — Crib 5 = Portus grade; Crib 7 & DIN 4102-1 B1 = Prosoft; Antiviral = **ISO 18184** (Mediflex); Antibacterial = Sanitized® / **JIS Z 2801** (+ ISO 846 A) [**corrected** from the page's ISO 20743 guess]; Martindale = **400,000 rubs / ISO 5470-2** [**corrected** from EN ISO 12947 — the textile method]; + the Crib/B1/Martindale FAQ answers. Medical (`collection.medical.json`) flame row filled per-product (Flora → BS 5852 Crib 5; Mediflex/Electra → EN 1021-1/2, FMVSS302, Cal. TB 117, NFPA 260) → **Medical now 0 markers**. Left 9 flagged on Performance Fabrics. **Then completed the case studies** — the "Specified, installed, still performing" section now carries **five anonymized real deployments** (emergency vehicles, pilot/passenger seating, motorcycle helmets, gym/fitness, office & industrial seating) from a client-supplied doc (`Cases Industrial Clients.docx`), with **no names/URLs and no invented figures** (identity kept secret — verified 0 client names on the page). **OEKO-TEX kept** per Ivana (it's in the PF brief). **PF now at 6 markers:** PFAS-free (×2) + 3 sector-tile links. Deployed via CLI, round-trip verified. Checklist refreshed: `briefs/performance-fabrics-TO-CONFIRM-checklist.md`.
* **SEO cannibalization — batch 2 (GSC-checked redirects).** Ran the narrow redirect-safety check in Search Console (6-mo, `www.monteirofabrics.com`) before firing. Restaurants (3 clicks / 1,680 impr, pos 31) vs Hospitality (4 / 1,277, pos 34) — both weak so the 301 is safe, but Restaurants carries the *larger* "restaurant/bar" impression base, so **retitled Hospitality → "Hospitality & Restaurant Upholstery Fabrics | Monteiro"** to absorb it. Train (0 clicks / 40 impr) — negligible. **301s created:** `/collections/restaurants-fabrics`→`/collections/hospitality-fabrics`, `/collections/train`→`/collections/mass-transportation` — both **need manual collection-unpublish** to activate (MCP blocks `publishableUnpublish`). **Held:** By-Industry + By-Application → Sectors — target `/pages/sectors` shows **0 impressions / 0 clicks in GSC** (not the live hub / unindexed) while the two sources rank (pos 8 & 12, ~280 impr combined); not redirecting ranking equity into a dead page until the real Sectors hub URL is confirmed. Train tiles remain on by-industry, by-application (retiring), `/pages/mobility`, `/pages/mission-vision` + `collection.train.json` — cleaned in the launch/IA pass. **Resolved same day:** client unpublished train & restaurants-fabrics and deleted healthcare-copia; **all 7 cleanup 301s verified live** (curl → 301 to the right targets). By-Industry + By-Application → Sectors **deferred to launch** — the real Sectors hub goes live with the proposal theme.
* **SEO cannibalization — batch 1 executed (retitles + duplicate cleanups), live.** Per `briefs/seo-cannibalization-remediation-plan.md`. **7 title-tag retitles** to de-overlap: `/collections/fashion` → "Vegan Leather for Bags, Footwear & Apparel | Monteiro" (`collectionUpdate`); pages (`global.title_tag` via `metafieldsSet`) `siesta-patio` (dropped the "Outdoor upholstery" term competing with `/collections/outdoor`), `mediflex`, `electra`, `origin`, `straw`, `amazónia` — all moved off the shared "Upholstery Fabrics | X Collection" / "Healthcare Fabrics | X" patterns to brand-led titles. **5 duplicate/junk 301s** (`urlRedirectCreate`): `healthcare-copia`→`/collections/medical`, `/pages/medical`→`/collections/medical`, the disabled medical-equipment ads LP→`/collections/medical`, `contact-us_`→`/pages/contact`, draft-old T&C→`/pages/terms-and-conditions`. `/pages/medical` unpublished via `pageUpdate isPublished:false` so its redirect is active; medical-equipment LP / contact-us_ / draft-old were already unpublished. **⚠ Manual step outstanding:** the MCP blocks `publishableUnpublish` (catalog safety) and collections have no `pageUpdate`-style toggle, so **`/collections/healthcare-copia` must be unpublished by hand** (admin → the collection → Online Store sales channel off) for its 301 to fire. **Decisions captured:** `/pages/quality`, `/pages/r-d`, `/pages/mission-vision` → **merge into About Us** (content move + 301, deferred — NOT retitled); `/pages/peel-collection` + the source collections **ocean / prosoft / portus / mediflex / peel → discontinued into CHANCE 2.0 grades at launch** (Bioblend=peel, Outdoor=ocean, Crib 7/B1=prosoft, Crib 5=portus, Mediflex=mediflex, Classic=chance) with 301s firing when CHANCE 2.0 goes live; the `chance-2-0` collection is the future hub — **left untouched**. Held for batch 2 (need GSC direction check / final Sectors URL): Restaurants→Hospitality, Train→Mass Transport, By-Industry + By-Application→Sectors. See [[collection-seo]].
* **Rebuild (SEO)**: **Medical collection page differentiated from Healthcare** (`collection.medical.json`, `/collections/medical`). Part of the cannibalization remediation — D1: Ivana wants a distinct **medical-grade** page that also captures the "medical"/"medical-grade" search demand "healthcare" misses. Repositioned from a near-Healthcare clone into a technical medical-grade page: H1 "Medical Fabrics" → **"Medical-Grade Upholstery Fabrics"**; added a **"What makes a fabric medical-grade?"** explainer and a **device/equipment applications** block (exam/treatment tables, dental chairs, OR/ICU seating, patient-handling, lab furniture); standards-aware FAQ that **defers specific certs to the data sheet (no invented claims)**; and **intent-separating cross-links** to `/collections/healthcare` in the intro, applications and FAQ so the two pages stop competing. Reused existing section types only (rich-text / spec-table / faq-schema / fabric-spec-icons); updated the CollectionPage + breadcrumb JSON-LD to the new name. Deployed via **Shopify CLI**, round-trip verified. Removed a duplicate FAQ ("What makes a fabric medical-grade?") that collided with the explainer, and added a **reciprocal Healthcare→Medical cross-link** (`collection.healthcare.json` intro) so the intent separation is bidirectional. **Live SEO applied** via `collectionUpdate` — title "Medical-Grade Upholstery Fabrics | Antimicrobial | Monteiro" + matching meta description. One existing `[TO CONFIRM]` (per-product flame retardancy) kept; no new placeholders. Range **confirmed at 3 products** (Mediflex/Electra/Flora), no expansion. See [[collection-seo]] · `briefs/seo-cannibalization-remediation-plan.md` (Appendix A).
* **Fix**: **Help & Contact page not rendering the brand font (KometPro).** Ivana's review flagged the "Help & Contact" page (`/pages/faq-s`, template `templates/page.faq.json`) showing a non-brand font. **Root cause**: every per-section `custom_css` block referenced font families that don't exist — `Kometpro-regular` / `kometpro-regular` (page H1, intro `<p>`, FAQ search input, accordion item titles + `<p>`) and `Kometpro-light` (each accordion group's `.h2`). The theme registers the brand font under a single `@font-face` family named **`KometPro`** (weights 100–700, in `assets/styles.css.liquid`); the `-regular`/`-light`-suffixed names match no face (CSS ignores case, not the suffix), so the browser fell back to `sans-serif`/default on the page's most prominent text. **Fix**: corrected all 24 references across the 9 sections to `'KometPro'`, keeping `font-weight: 300` on the `.h2` "light" rule (300 = the Light face). **Deploy**: pushed to the sandbox via **Shopify CLI** (`shopify theme push --only templates/page.faq.json --theme 195386114425 --nodelete`) — the custom-app token still lacks `read_themes`/`write_themes` (so `deploy-files.mjs`/`pull-files.mjs` remain blocked), but the CLI is logged in with theme write access and reads local files directly (cleaner than the MCP `themeFilesUpsert` fallback). Verified the corrected file on the remote theme.
* **Fix (same bug, 3 more templates)**: cleaned the identical undefined-family references in `page.taxonomia.json` (3× `KometPro-Regular` on `h2`, plus `.line-1` `kometpro-extralight` → `'KometPro'` keeping `font-weight: 100`), `page.event.json` (`h1` + `h2` `KometPro-Regular`), and `collection.colecoes-taxonomia.json` (`h2` `KometPro-RegularItalic`). All → `'KometPro'`; the **italic was dropped** (rendered upright) per client decision — there is no italic KometPro face. Pushed via CLI, round-trip-verified (0 stale refs server-side).

CLI push doesn't refresh `.deploy-state` snapshots.

## 2026-07-02

* **Creation**: **Distinct Technical Hub blog header.** The Technical Hub listing (`/blogs/technical-hub`) was rendering the shared News template (photographic "NEWSROOM / News" hero + trade-fair subhead). Gave it its own template `templates/blog.technical-hub.json` (hero → `main-blog` w/ filter, `intro_text` cleared, title off → newsletter "Get new technical guides") and set the blog `templateSuffix = technical-hub` (`blogUpdate`). New section `sections/technical-hub-hero.liquid`: a flat brand-blue (`#2F519B`) "Resource Library" band with a subtle blueprint grid (CSS `linear-gradient` + radial `mask`), an uppercase letter-spaced eyebrow + a topics line ("Fire safety · Cleaning & hygiene · Healthcare · Certification"), and the page H1 (explicit white — the theme's `h1` colour was overriding inheritance) — deliberately different from the editorial Newsroom hero. Straight corners, configurable (eyebrow/title/subhead/topics/padding/colours). **Deploy note**: mid-session the `.shopify-token.json` custom-app token **lost `read_themes`/`write_themes`** (the "create page" app was updated/rescoped), so `deploy-files.mjs` failed; deployed instead via the **Shopify MCP connector** (`themeFilesUpsert` on the unpublished theme). Because that bypassed `deploy-files.mjs`, no `.deploy-state` snapshot exists for these two files yet — **run `pull-files.mjs` once the app token's theme scopes are restored** to re-sync drift snapshots. (Range gotcha again: `pad_mobile` default must be a step in its range → 70, not 72.)
* **Creation**: **Technical Hub pillar — "The Complete Guide to Healthcare Upholstery Fabrics"** (article id 1007373713785, `/blogs/technical-hub/complete-guide-to-healthcare-upholstery-fabrics`, **published**). The white-paper/pillar asset the 5 spoke articles link up into — a long-form, crawlable specification guide (risk framing → 5 core requirements in depth → certifications glossary → cleaning matrix table → specify-by-furniture-type → **reusable toolkit** [spec checklist, cleaning/disinfectant matrix, standards glossary, cert quick-reference] → About Monteiro → FAQ), deep-linked to all 5 spokes + healthcare/medical collections + performance-fabrics + contact. Written to E-E-A-T, **no `[SPECIALIST]` markers** (accurate on public standards — BS 5852 Crib 5/7, DIN 4102-1 B1, EN 13501-1, ISO 22196/20743, EN ISO 12947, REACH, OEKO-TEX; no invented Monteiro figures). SEO title/desc + curated `related_products` (Mediflex/Electra/Flora/Prosoft) set; inherits breadcrumbs + connection bands from `article.json`. Built as an **on-site pillar rather than a gated PDF** (gating hurts the SEO/AI-citation goal; a designed PDF still needs the specialist's first-hand data) — the Performance Fabrics **featured "white paper" card** (`tech-resources` r0 in `page.performance-fabrics.json`) was repointed from the contact placeholder to the guide, CTA "Download the guide"→**"Read the guide"**, meta "Free PDF"→"Complete guide · 15 min read" (deployed; template had drifted from theme-editor edits → pulled first). *A gated downloadable PDF remains a future packaging step once the specialist enriches the content.*
* **Content/Update**: **Technical Hub — 5 articles finalised** (`crib-5-vs-crib-7`, `choosing-fabrics-for-healthcare`, `healthcare-furniture-requirements`, `bleach-cleaning-fabric-guide`, `understanding-b1-certification`). Per Ivana's process (AI + established knowledge writes the draft, specialist enriches with first-hand data later), each was rewritten to the **E-E-A-T** framework — snippet-ready short answer up top, H2/H3 structure, comparison tables/checklists, FAQ, internal links — accurate on public standards (BS 5852 Crib 5/7, DIN 4102-1 B1, EN 13501-1, ISO 22196/20743, EN ISO 12947 Martindale, REACH). Then, per client request, **all 32 `[SPECIALIST: …]` placeholders were removed** and the surrounding copy reflowed so nothing dangles and no unverified figure is claimed (proprietary numbers softened to "stated in our written specification / ask our technical team"). **Connecting sections**: these articles already inherit the shared `article-connections` band from the default `article.json` (Related fabrics → Continue reading → CTA); strengthened by setting a curated **`custom.related_products`** metafield per article (list.product_reference) so "Related fabrics" shows hand-picked grades — crib/B1 → Portus + Prosoft + Mediflex; healthcare set → Mediflex/Flora/Electra/Glam/Laguna. **Breadcrumbs**: wired the orphaned `snippets/breadcrumbs.liquid` (self-contained `{% style %}`, `BreadcrumbList` microdata) into `sections/main-article.liquid`, so **all** articles now render `Home / <Blog> / <Title>`. Deployed section + snippet; `templates/article.json` had drifted (theme-editor edits to the connections settings — `default_collection`, CTA link) → pulled and preserved. Articles remain **published**.
* **Creation**: Two new **sectors** ([Sector & Collection SEO pages](/website/sector-pages-seo.md)). **Medical** (`/collections/medical`, id 704259621241) — reused the existing medical collection (Mediflex/Electra/Flora), **removed the `/collections/medical → /collections/healthcare` 301 redirect** (client-approved), set `templateSuffix medical` + SEO, built `collection.medical.json`. **Train** (`/collections/train`, id 704621740409) — **new collection** with the same 6 products as Mass Transportation, rail SEO copy, `templateSuffix train`, published to Online Store, built `collection.train.json`. Both added as tiles on the "Our Sectors" page (`page.all-collectby-industry.json` → `collection-list`). Specs `[TO CONFIRM]` (Medical fire-retardancy; Train EN 45545-2 + abrasion); Train collection has no image yet. Notion "Page Review" rows added (Section = Our Sectors, To review).
* **Creation/Rebuild**: [Contact Us page](/website/contact-page.md) at `/pages/contact` (sandbox). Cut from a sprawling 9-section layout to **hero → `contact-block` → map** (per Ivana: "direct contact, apart from Help & Contact" — FAQ accordions, slideshow, app embed and two duplicate "By email" blocks removed). New section `sections/contact-block.liquid`: two-column **B2B qualifying form** (native `{% form 'contact' %}` — Name/Company/Email/Phone/Country/Sector-dropdown/Message; custom `contact[*]` fields are emailed) + a "reach us directly" column (email/phone/hours); centered (`max-width 1060`), straight corners, brand-blue. **Gotcha fixed**: the theme's `selectReplace()` wrapped the `<select>` in a `.pretty-select` (misaligned) → added `class="original-selector"` to skip it, then overrode the theme's `.original-selector{display:none}` with a higher-specificity `display:block` to keep it visible. Side email set to `hello@monteirofabrics.com` (client). *Pending: route submissions to Ivana's forthcoming dedicated inbox (native form emails the store contact email).* Notion "Page Review" *Contact Us* row updated (preview + Changes requested). The other contact page (`page.contact-form1.json`) + shared `main-contact` left untouched.
* **Update**: [Performance Fabrics landing page](/website/performance-fabrics-page.md) — first round of **Ivana's review** (Notion "Page Review") incorporated. **CTAs**: hero + closing buttons "Request … samples" → **"Get in touch"** (no sample offer for this client type; a dedicated technical inbox is Ivana's to set up, then CTAs re-point). **Story**: added **"backed by 60 years of expertise"**. **Capacity** band → **4-up** with a leading **"60 years / of expertise"** stat card. **New `lab` section** ("Tested inside and out", eyebrow "Quality & testing") with Ivana's approved copy — internal lab **and** certified third-party labs + warranty — placed after the certs table. **Removed** the CHANCE 2.0 tie-in section (page shouldn't communicate collections). **Bug fix**: capacity card "In-house R&amp;D" was double-encoding (grade-cards title is `| escape`d) → literal `&` (**In-house R&D**). **Left-aligned** `grade-cards` card titles (were inheriting the theme's centered headings — shared change, also affects CHANCE 2.0 grade cards). *Still pending: the R&D & pilot-production section (awaiting facts).* Deployed via `deploy-files.mjs` (drift-guarded; pulled first each time).
* **Update**: **Custom Solutions / Tailormade** page (`page.tailor-made.json`). Added **outline (secondary) CTAs** to the three highest-intent "Media with text" (`image-with-text`) blocks — Custom Fabric Production → *Start a custom project*, Consultation Services → *Book a consultation*, Bulk Order Customization → *Enquire about bulk orders* — all → `/pages/contact`. No code change (the section already supports buttons); the other three service blocks stay text-only.
* **Update**: **CHANCE Classic** collection page (`collection.chance-classic.json`) — removed the **filter bar** (main-collection `filter_mode_desktop` / `filter_mode_mobile` → `none`); the 236-colour product grid stays. Confirmed the Classic `templateSuffix` handle/translation conflict is **resolved** (`chance` → `chance-classic`), so `/collections/chance` renders the grade template directly (no `?view=` workaround). See [CHANCE 2.0](/website/chance-2-0.md).
* **Collaboration**: **"Page Review"** Notion database refreshed for Ivana — added rows for the **Technical Hub blog + 5 articles** and filled **Preview links + status** for the Performance Fabrics landing page and all **7 CHANCE 2.0 rows** (hub + Classic + 5 grades), preserving existing feedback notes.

## 2026-06-30

* **Creation/Rebuild**: [Monteiro Performance Fabrics landing page](/website/performance-fabrics-page.md) at `/pages/performance-fabrics` (sandbox). The earlier bespoke **`pf-*`** iteration was reset (template found as an empty placeholder; the `pf-*` sections/snippets/`performance-fabrics.css` no longer exist) and **rebuilt from the redesign's reusable sections** on a light, **straight-corner**, brand-blue (`#2F519B`) design. `page.performance-fabrics.json` order: `image-with-text-overlay` hero (bg image `fabricante_cadeiras_estadio_xxx2.png`, H1) → `rich-text` story → `grade-cards` capacity (6M m*/Portugal/R&D) → `performance-tech` band (6 `perf-*.svg`) → `fabric-spec-icons` cert strip → `spec-table` certifications (12 rows) → `grade-cards` 7 sector tiles → `rich-text` CHANCE 2.0 tie-in → `rich-text` case studies → `tech-resources` → `faq-schema` → `rich-text` CTA. Shopify page id 715008672121, published, `templateSuffix performance-fabrics`, SEO title/desc; live falls back to the default template (intro only). All certification values + case studies + 6M figure are `[TO CONFIRM]` placeholders.
* **Creation**: **Technical Hub** content cluster. New section `sections/tech-resources.liquid` (featured white-paper card + article grid + `ItemList` JSON-LD) on the Performance Fabrics page. New blog **`/blogs/technical-hub`** (id 125895213433, comments closed, SEO meta) + **five draft articles** (`crib-5-vs-crib-7`, `choosing-fabrics-for-healthcare`, `healthcare-furniture-requirements`, `bleach-cleaning-fabric-guide`, `understanding-b1-certification`) — handles match the section's card links, each with a query-answering body, internal links to the pillar page, and SEO title/description metafields. Kept **unpublished** (store-level resources stay off live until launch); facts `[TO CONFIRM]`. White-paper lead magnet deferred. Also squared the corners on `tech-resources` + `performance-tech` to match the house style.
* **Creation/Update**: [News page redesign & SEO hygiene](/website/news-page-redesign.md). Rebuilt the `/blogs/news` listing on the sandbox into an editorial **Newsroom** — crawlable **chip filter** (replaces the dropdown), **category pills**, **read time**, **excerpts**, and a **featured-first** newest post. Added `Blog` + `ItemList` + `BreadcrumbList` JSON-LD (new `structured-data-blog` snippet) and upgraded the article JSON-LD (`https://schema.org`, added `BreadcrumbList`). Hero "NEWS & EVENTS" → **"News"**; dropped the stale/disabled trade-fair section ("Events" deferred as a destination). Files: `templates/blog.json`, `sections/main-blog.liquid`, `snippets/article-block.liquid`, `snippets/structured-data-blog.liquid`, `snippets/structured-data-article.liquid`, `assets/styles.css.liquid` (deployed via `deploy-files.mjs`).
* **Update (LIVE)**: SEO hygiene via `scripts/news-seo-hygiene.mjs`. Set the **News blog** SEO title + meta description (were empty). Normalised the **tag taxonomy** to a clean 7 — Collections, Company, Events, Hospitality, Materials, Projects, Sustainability — killing the `SUSTAINABLILITY` typo + `HOTEL`/`Restaurants`/`delivery`/`manufacturers` noise, and tagged the ~22 untagged articles (all 35 re-tagged by ID). **Backfilled meta descriptions** on the 8 articles that had none. 0 failures.

## 2026-06-29

* **Creation**: Built the **CHANCE 2.0** family on the sandbox theme. CHANCE becomes an umbrella of six performance grades sharing one engraving (Classic = Chance, Mediflex = Mediflex, Outdoor = Ocean, Bioblend = Peel, Crib 7/B1 = Prosoft, Crib 5 = Portus). Evolved the existing `/pages/chance-collection` editorial page into the CHANCE 2.0 **hub** (hero + a new `grade-cards` section + shared credentials + comparison matrix + 236-colour story + FAQ/JSON-LD), so the page keeps its ranking authority.
* **Creation**: Created five new Shopify collections (`chance-mediflex` 704468517241, `chance-outdoor` 704468550009, `chance-bioblend` 704468582777, `chance-crib-7-b1` 704468615545, `chance-crib-5` 704468648313), each holding its product line with SEO + `templateSuffix`. Built six grade collection templates carrying the real **DC 073.01** catalogue specs (composition, weight, fire list, biological, biobased).
* **Update**: The five single-product grade pages now display the **product directly** (`featured-product`: media + colour swatches + buy/sample button) instead of a collection grid + filter bar; the product's own title block is omitted so the old names don't resurface. Classic keeps the grid (236-colour palette across 7 products).
* **Update**: Added CHANCE 2.0 to the redesign nav (`mf_june_menu`) — "Collections" is now a curated dropdown (CHANCE 2.0 · Straw · Origin · Amazónia · Electra · Siesta & Patio); and added a **CHANCE 2.0 feature** to the homepage (`templates/index.json`, after the differentiators). The live `main-menu` is untouched.
* **Note**: Classic stays on `/collections/chance`; its `templateSuffix` (chance-classic) is **blocked by a handle/translation conflict** on the `chance` collection — previewed via `?view=chance-classic`, to be resolved at publish. The old standalone product/page URLs (mediflex, ocean, peel, prosoft, portus) will be **301'd to the new grade slugs at publish** (staged, not yet fired — firing now would bounce live product pages). Sector-page links to the old names still pending repoint.

## 2026-06-26

* **Creation**: Added the `website/` folder to record website changes as evidence of work done — the
  [June 2026 Website Redesign](/website/june-2026-redesign.md) initiative and its two workstreams below.
* **Creation**: [Sector & collection SEO pages](/website/sector-pages-seo.md) — rebuilt **18**
  `/collections/` pages with keyword H1s, crawlable spec tables, sector FAQs and
  FAQPage / BreadcrumbList / CollectionPage JSON-LD; built two reusable theme sections
  (`spec-table`, `faq-schema`).
* **Update**: Applied SEO title + description to all 18 collections; created 301 redirects
  (`/collections/marine-interior` → `/collections/marine`, `/collections/medical` → `/collections/healthcare`);
  set each collection's `templateSuffix` so the new templates render on the normal URL in the preview.
* **Creation**: [Signature collection landing pages](/website/signature-collection-pages.md) — recorded
  the eight editorial `/pages/` rebuilds and the bespoke sections (`process-timeline`, `fabric-anatomy`,
  `palette-forest`, `decor-motif`).
* **Note**: All changes staged on the unpublished **CLAUDE_MF_JUNE_PROPOSTA** theme; live
  **Showcase 7.0** untouched. Per-page review tracked in the Notion "Page Review" database.
